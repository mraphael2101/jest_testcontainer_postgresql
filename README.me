yarn init
// press enter until success.
yarn add pg
yarn add -D typescript ts-jest testcontainers @types/jest @types/pg
mkdir __tests_
// jest will search test cases in __tests__ directory by default.
npm install --save-dev jest